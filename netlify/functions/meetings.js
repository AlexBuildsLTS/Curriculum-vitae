// netlify/functions/meetings.js
exports.handler = async (event, context) => {
    // parse method, do some routing based on event.httpMethod
    // connect to DB, run queries
    // return { statusCode, body }
};
